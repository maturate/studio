# Packages

Shared packages for the app should live here.

Planned packages:

- `shared` — shared types, utilities, and schemas
- `model-registry` — provider/model metadata and pricing rules
- `workflow-engine` — workflow graph contracts and execution helpers
- `context-engine` — `superOS` knowledge ingestion and retrieval logic
