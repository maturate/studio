# Workers

Background execution should live here.

Planned worker groups:

- `media` — provider-facing generation and transform jobs
- `workflow` — graph execution, retries, batch runs, and orchestration
